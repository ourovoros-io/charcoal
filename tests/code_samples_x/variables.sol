// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

address constant c  = address(0);

contract Variables {
    address z = address(0);
    function variables() pure public {
        uint256 a = 0;
        address x = address(0); 
    }
}